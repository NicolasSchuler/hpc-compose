interface:
  display_name: "hpc-compose"
  short_description: "Author safe hpc-compose Slurm workflows"
  default_prompt: "Use $hpc-compose to adapt this repository with version-matched guidance, static JSON checks, and explicit authorization boundaries."
