interface:
  display_name: "hpc-compose"
  short_description: "Set up and adapt hpc-compose on Slurm"
  default_prompt: "Use $hpc-compose to set up hpc-compose for my Slurm cluster and adapt this repository for a first safe run."
