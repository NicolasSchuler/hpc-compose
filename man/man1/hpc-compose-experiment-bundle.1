.\" DO NOT EDIT. Generated by `cargo run --features manpage-bin --bin gen-manpages`
.TH "HPC\-COMPOSE\-EXPERIMENT\-BUNDLE" "1" "" "hpc\-compose 0.2.0" "User Commands"
.SH NAME
hpc\-compose\-experiment\-bundle \- Emit a paper\-ready reproducibility archive for a tracked run
.SH SYNOPSIS
.nf
hpc\-compose experiment bundle [OPTIONS] [JOB_ID]
.fi
.SH DESCRIPTION
.PP
Bundle one tracked run into a citeable tar.gz (or a directory with \-\-dir): the compose spec, the resolved config snapshot, the rendered sbatch, provenance (git SHA + dirty flag + image references), the sweep manifest with seeds, metrics (CSV + raw JSONL), checkpoint attempt history, and a generated methods appendix (README.md) with a MANIFEST.json carrying per\-file sha256. Image entries are references as recorded at submit time, not content digests, and are not resolved against any registry. Defaults to the latest tracked run. Missing ingredients degrade with a warning and a MANIFEST `missing[]` entry unless \-\-strict is given, in which case the command fails after reporting them.
.SH OPTIONS
.TP
\fIJOB_ID\fR
Tracked Slurm job id to bundle; defaults to the latest tracked run.
.TP
\fB-f\fR, \fB--file\fR \fIFILE\fR
Compose specification file to read; if omitted, use the active context compose file or fall back to compose.yaml.
.TP
\fB--output\fR \fIPATH\fR
Output archive path (or directory with \-\-dir); defaults to experiment\-bundle\-<job_id>.tar.gz in the current directory.
.TP
\fB--dir\fR
Write an unpacked bundle directory instead of a tar.gz archive.
.TP
\fB--strict\fR
Fail (non\-zero) if any ingredient is missing, after reporting what is absent.
.TP
\fB--format\fR \fIFORMAT\fR
Output format. Possible values: text, json.
.TP
\fB--color\fR \fIWHEN\fR
Controls colored output. Default: auto. Possible values: auto, always, never.
.TP
\fB--quiet\fR
Suppress progress indicators and non\-essential labels.
.TP
\fB--profile\fR \fINAME\fR
Profile name to load from .hpc\-compose/settings.toml.
.TP
\fB--settings\-file\fR \fIPATH\fR
Explicit settings file path; defaults to upward search for .hpc\-compose/settings.toml.
.TP
\fB-h\fR, \fB--help\fR
Print help (see a summary with '\-h').
.SH EXAMPLES
.nf
hpc\-compose experiment bundle
hpc\-compose experiment bundle 12345 \-\-output run.tar.gz
hpc\-compose experiment bundle 12345 \-\-dir
hpc\-compose experiment bundle \-\-strict \-\-format json
.fi
.SH SEE ALSO
hpc\-compose(1), hpc\-compose\-experiment(1), hpc\-compose\-experiment\-note(1), hpc\-compose\-experiment\-show(1), hpc\-compose\-experiment\-tag(1)
